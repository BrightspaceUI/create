/*
    __buildConfig__ is being replaced at build time with the appropriate
    configuration for the environment.

    Do not reference __buildConfig__ directly outside of this file in order to
    avoid further lint disabling. Import this file instead.

    See shared.config.js for __buildConfig__ definition.
*/
export default __buildConfig__; // eslint-disable-line no-undef;
